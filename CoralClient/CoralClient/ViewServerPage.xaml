<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CoralClient.ViewModel"
             x:DataType="vm:ViewServerViewModel"
             x:Class="CoralClient.ViewServerPage"
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="130, *">
        <BoxView Grid.RowSpan="2">
            <BoxView.Background>
                <LinearGradientBrush>
                    <GradientStopCollection>
                        <GradientStop Color="#161616" Offset="0"/>
                        <GradientStop Color="#30343F" Offset="1"/>
                    </GradientStopCollection>
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>
        <Grid RowDefinitions="Auto, *, *">
            <Path Data="M0,0H375V129.651s-9.025,2.382-55.426-5.3-70.141-41.09-130.3-41.176-83.544,33.5-130.893,41.176S0,129.651,0,129.651Z"
                  Aspect="Fill" Fill="#eda1ab"/>

            <StackLayout Grid.Row="0" Orientation="Horizontal" Margin="25" Spacing="20" VerticalOptions="Start">
                <ImageButton Source="back.png" BackgroundColor="Transparent" WidthRequest="20" HeightRequest="20" 
                    HorizontalOptions="Start" VerticalOptions="Center" Command="{Binding BackCommand}"/>
                <Label Text="View Server" TextColor="Black" FontSize="20" FontAttributes="Bold" VerticalOptions="Start"/>
            </StackLayout>

            <Grid Grid.Row="1" BackgroundColor="Black">
                <Image Source="{Binding ServerProfile.Image}" Aspect="AspectFit" />
                <AbsoluteLayout>
                    <BoxView x:Name="OverlayBoxView"
                             BackgroundColor="#80000000"
                             IsVisible="{Binding IsEditing, Mode=TwoWay}"
                             AbsoluteLayout.LayoutFlags="All"
                             AbsoluteLayout.LayoutBounds="0,0,1,1" />
                    <StackLayout Orientation="Vertical" VerticalOptions="Center" HorizontalOptions="Center">
                        <Button Text="Change Image" Command="{Binding EditImageCommand}" IsVisible="{Binding IsEditing, Converter={StaticResource InverseBoolConverter}}" />
                    </StackLayout>
                </AbsoluteLayout>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" TextColor="White" Text="URI:" FontAttributes="Bold" />
                <Entry Grid.Row="0" Grid.Column="1" TextColor="White" Text="{Binding ServerProfile.Uri}" />
                <Button Grid.Row="0" Grid.Column="2" Text="Edit" Command="{Binding EditUriCommand}" />

                <Label Grid.Row="1" Grid.Column="0" TextColor="White" Text="Minecraft Port:" FontAttributes="Bold" />
                <Entry Grid.Row="1" Grid.Column="1" TextColor="White" Text="{Binding ServerProfile.MinecraftPort}" />
                <Button Grid.Row="1" Grid.Column="2" Text="Edit" Command="{Binding EditMinecraftPortCommand}" />

                <Label Grid.Row="2" Grid.Column="0" TextColor="White" Text="RCON Port:" FontAttributes="Bold" />
                <Entry Grid.Row="2" Grid.Column="1" TextColor="White" Text="{Binding ServerProfile.RconPort}" />
                <Button Grid.Row="2" Grid.Column="2" Text="Edit" Command="{Binding EditRconPortCommand}" />

                <Label Grid.Row="3" Grid.Column="0" TextColor="White" Text="RCON Password:" FontAttributes="Bold" />
                <Entry Grid.Row="3" Grid.Column="1" TextColor="White" Text="{Binding ServerProfile.Password}" />
                <Button Grid.Row="3" Grid.Column="2" Text="Edit" Command="{Binding EditPasswordCommand}" />
            </Grid>

        </Grid>
    </Grid>
</ContentPage>